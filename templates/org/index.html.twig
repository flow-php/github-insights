{% extends 'base.html.twig' %}

{% block title %}Organization - {{ org }}{% endblock %}

{% block body %}

<div class="wrapper">
    <h1>{{ org }}</h1>
    <a href="{{ path("app_home") }}">Back</a>
    <h4>Repositories:</h4>
    <ul>
        {% for repo in repos %}
            <li>
                <a href="{{ path("app_pr", {org: org, repo: repo}) }}">
                    {{ repo }}
                </a>
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
